description = 'example :: preinit-testcontainers-clickhouse (Maven Central consumer)'

dependencies {
	implementation "com.sviat-tech:preinit-testcontainers-clickhouse:${rootProject.version}"

	testImplementation libs.clickhouse.jdbc
}
