description = 'example :: preinit-testcontainers-postgresql (Maven Central consumer)'

dependencies {
	implementation "by.macmonitor:preinit-testcontainers-postgresql:${rootProject.version}"

	testImplementation libs.postgresql.jdbc
}
